{
  "name": "Madara Uchiha Rename Bot",
  "description": "A Pyrogram bot to rename and manage Telegram files with aria2 and MongoDB support.",
  "repository": "https://github.com/yourusername/yourrepo",
  "keywords": ["telegram", "pyrogram", "bot", "rename", "aria2", "mongodb"],
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID from my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API HASH from my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Your bot token from @BotFather",
      "required": true
    },
    "OWNER_ID": {
      "description": "Your Telegram user ID (as int)",
      "required": true
    },
    "MONGO_URL": {
      "description": "MongoDB URI string for saving user data",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
  "stack": "container"
}
